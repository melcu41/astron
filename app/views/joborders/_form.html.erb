<%= form_for(@joborder) do |f| %>
  <% if @joborder.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@joborder.errors.count, "error") %> prohibited this joborder from being saved:</h2>

      <ul>
      <% @joborder.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :joborderid %><br />
    <%= f.text_field :joborderid %>
  </div>
  <div class="field">
    <%= f.label :desc %><br />
    <%= f.text_field :desc %>
  </div>
  <div class="field">
    <%= f.label :customer_id %><br />
	<%= @customers = Customer.find(:all, :order => "name") 
    collection_select(:joborders, :customer_id, @customers, :id, :name, options ={:prompt => "Select a customer"}, :class =>"customer") %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
